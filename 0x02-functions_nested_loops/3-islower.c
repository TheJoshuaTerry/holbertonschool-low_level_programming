#include "main.h"
#include <stdio.h>
/**
 * _islower - see description
 * @c: number
 * Description: Print alpha 10 times
 * Return: Always 0 (Success)
*/
int _islower(int c)
{
	if (c > 96)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
